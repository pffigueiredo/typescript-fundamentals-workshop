// Run `npm run tsc:samples` on the terminal to see the compiled output.

// A lot of comments
// Here
// There
let a = 123;

function myFn(arg) {
  return arg;
}

type MyType = string;
type MyType2<T> = T extends string ? 'yes' : 'no';

// More Comments
