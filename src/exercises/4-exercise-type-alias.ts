// Create a type alias with a `string` type and use it for the function `arg`
function fn(arg) {
  return arg.length;
}

// Create a type alias with a `boolean` type, assign it to `aliasB` and assign it a value
let aliasB; // assign a type here
aliasB = ??; // assign a value here
